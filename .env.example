# Copy to .env.local and fill in. Do NOT put real values here — this file is committed.

# Transactional email (SendGrid, a Twilio product) — used by the /api/contact lead form.
SENDGRID_API_KEY=

# Optional overrides for the contact form (defaults are baked into src/app/api/contact/route.ts):
# SENDGRID_LEAD_TEMPLATE_ID=
# LEAD_FROM_EMAIL=noreply@gostaffer.com
# LEAD_TO_EMAILS=leads@gostaffer.com
